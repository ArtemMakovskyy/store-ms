spring:
  datasource:
    url: jdbc:tc:postgresql:15.3:///products
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  test:
    database:
      replace: none